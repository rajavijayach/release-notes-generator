{
  "name": "release-notes-generator",
  "version": "1.0.0",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "releaseNotesServer": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}index.js"],
      "cwd": "${extensionPath}"
    }
  },
  "settings": [
    {
      "name": "GitHub Token",
      "envVar": "GITHUB_TOKEN",
      "sensitive": true,
      "description": "Optional. Personal access token for fetching GitHub Pull Request data."
    },
    {
      "name": "Jira Token",
      "envVar": "JIRA_TOKEN",
      "sensitive": true,
      "description": "Optional. Jira API token for fetching ticket titles and descriptions."
    },
    {
      "name": "Jira Base URL",
      "envVar": "JIRA_BASE_URL",
      "sensitive": false,
      "description": "Optional. Your Jira instance URL (e.g. https://yourcompany.atlassian.net)."
    }
  ]
}
